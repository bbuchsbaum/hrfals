% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hrfals_lss.R
\name{hrfals_lss}
\alias{hrfals_lss}
\title{Beta-series estimation using fast LSS}
\usage{
hrfals_lss(
  cf_fit,
  events,
  fmri_data_obj,
  confound_obj = NULL,
  baseline_model = NULL,
  mode = c("auto", "shared", "voxel"),
  whitening_matrix = NULL,
  ...
)
}
\arguments{
\item{cf_fit}{An object of class `hrfals_fit` as returned by
[hrfals()] or [fmrireg_cfals()].}

\item{events}{A `data.frame` with onset information or an
`fmrireg::event_model`.}

\item{fmri_data_obj}{BOLD data matrix or `fmrireg::fmri_dataset`
matching the data used for CF-ALS.}

\item{confound_obj}{Optional confound matrix with the same number of
rows as `fmri_data_obj`.}

\item{baseline_model}{Optional baseline model for confound projection.}

\item{mode}{Character string specifying the LSS kernel variant.
If "auto" (default) the function selects "shared" when
a single HRF is present in `cf_fit$h_coeffs` and "voxel" otherwise.}

\item{whitening_matrix}{Optional whitening matrix applied to all
design matrices before running the kernel.}

\item{...}{Additional arguments passed to [fastlss_shared()] or
[fastlss_voxel()].}
}
\value{
An object of class `fastlss_fit` containing the trial
  coefficient matrix and metadata.
}
\description{
Builds trial regressors from a CF-ALS fit and event information and
calls \code{fastlss_shared()} or \code{fastlss_voxel()} as appropriate.
}
